{
  "description": "prevent-main-commit hook from awesome-claude-code",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/prevent-main-commit.sh\""
          }
        ]
      }
    ]
  }
}
